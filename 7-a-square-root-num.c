// 7.a Square root of a number
#include <stdio.h>
#include <math.h>
int main()
{
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    printf("Square root of %d is %f", n, sqrt(n));
    return 0;
}